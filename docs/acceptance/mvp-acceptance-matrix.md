# 公开验收范围

公开仓通过 TypeScript 编译、边界检查、秘密检查和自动化测试验证以下能力：

- 项目白名单与移动身份控制；
- 会话、Turn 队列、停止、关闭和恢复的状态语义；
- 原生 Agent 审批的受控转发；
- 渠道消息去重与回复失败不重复执行；
- 本机 IPC、配置、日志和更新产物的基本安全边界；
- Codex、Grok、Claude 适配器的兼容性契约。

真实账号、设备、渠道会话、发布资格与内部验收过程不包含在公开仓中。运行 `npm run check` 可执行
公开的构建、静态检查与测试套件。
